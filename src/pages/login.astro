---
// File: src/pages/login.astro
// Perbaikan: Menggunakan alias path untuk impor yang lebih konsisten dan stabil.

// PERBAIKAN: Ubah path relatif menjadi alias path.
import MainLayout from '@/layouts/MainLayout.astro';
import AuthForm from '@/components/AuthForm.jsx';
---

<MainLayout title="Login / Daftar - BJS RACING STORE">
    <div class="container mx-auto px-4 py-16">
        <h1 class="text-3xl font-bold text-center mb-8">Akun Saya</h1>

        {/* Logika render tetap sama, tidak ada perubahan di sini. */}
        <AuthForm client:only="react" />

    </div>
</MainLayout>